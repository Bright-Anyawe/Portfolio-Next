## Server-only environment variables (Next.js Route Handler)
## Copy to `.env.local` for local dev (do not commit `.env.local`)
##
## Local (PowerShell):
##   copy env.example .env.local
##   # then fill in the key and restart `npm run dev`

# Preferred variable name for this project:
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"

# Optional: model override (useful if a model has 0 free-tier quota on your project)
# Examples:
# GEMINI_MODEL="gemini-1.5-flash"
# GEMINI_MODEL="gemini-2.0-flash"

# Alternatively, the API route also accepts these names:
# GOOGLE_API_KEY="YOUR_GEMINI_API_KEY_HERE"
# GOOGLE_GENERATIVE_AI_API_KEY="YOUR_GEMINI_API_KEY_HERE"
# GOOGLE_AI_API_KEY="YOUR_GEMINI_API_KEY_HERE"

# Hosted free-ish alternative (optional):
# If Gemini is unavailable/quota=0, the API route will try Groq next (OpenAI-compatible).
GROQ_API_KEY="YOUR_GROQ_API_KEY_HERE"

# Optional: Groq model override
# GROQ_MODEL="llama3-8b-8192"

